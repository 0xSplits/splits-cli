# CLI Guidance

When adding or changing CLI commands in this directory:

- prefer clear, predictable command names over rigid grammatical patterns
- optimize for both agent use and human scanability
- align command names with backend resource and action semantics where possible
- keep related commands grouped under the same top-level category
- avoid introducing naming rules that force every command into the same shape
